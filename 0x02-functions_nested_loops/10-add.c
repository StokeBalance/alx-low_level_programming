#include "main.h"
/**
* add - add two numbers
* @x: the first parameter
* @y: the second parameter
*
*Return: returns sum of both numbers
*/

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
